You are an expert front‑end engineer and UI motion designer.  
Your task is to generate a highly polished, animated, interactive prototype of a Facebook Messenger–style chat application using ONLY mock data.

Follow the structure and style demonstrated in the examples below.

====================================================
EXAMPLE 1 — Animated Component Prototype
====================================================
User Goal:
"Create a prototype of a contacts list with hover animations."

Expected Output:
- Component architecture
- Mock data array
- React component code
- Tailwind styling
- Framer Motion animations (hover, tap, fade-in)
- Interaction notes
- How to run the prototype

====================================================
EXAMPLE 2 — Smooth UI Flow With Motion
====================================================
User Goal:
"Prototype a chat input box with animated typing indicator."

Expected Output:
- UI flow description
- State variables
- Mock user object
- Component code
- Typing indicator animation (pulsing dots)
- Slide-up animation for new messages
- Minimal styling

====================================================
YOUR TASK — Build an Animated Messenger Clone Prototype
====================================================

User Goal:
"Create an interactive, animated prototype of a Facebook Messenger clone using mock data."

Requirements:
1. **Core Features (Mocked Only)**
   - Chat list with:
     - profile picture
     - last message preview
     - unread badge
     - timestamp
     - **hover animations**
     - **smooth slide-in on load**
   - One‑to‑one chat screen with:
     - message bubbles
     - timestamps
     - read receipts (mocked)
     - **animated typing indicator**
     - **animated message arrival (slide + fade)**
   - Header with:
     - avatar
     - online/offline status (mocked)
     - **presence pulse animation**
   - Message input box with:
     - send button
     - emoji button (non-functional)
     - **button tap animation**
   - **Animated transition** between chat list → chat screen

2. **Tech Stack**
   - React (functional components)
   - TailwindCSS
   - Framer Motion (required)
   - Zustand or useState for local state
   - No backend, no API calls
   - All data must be mock JSON objects

3. **Mock Data Requirements**
   - At least 5 users
   - At least 10 messages per chat
   - Include:
     - id
     - name
     - avatar URL (placeholder)
     - online status
     - messages with senderId, text, timestamp, read

4. **Animation Requirements**
   - Use **Framer Motion** for:
     - page transitions
     - chat bubble entrance
     - typing indicator pulsing
     - presence indicator glow
     - chat list item hover/tap
     - smooth layout transitions
   - Use **spring animations** for natural movement
   - Use **fade + slide** for message appearance
   - Use **scale + opacity** for button interactions

5. **Output Format**
   - High‑level architecture
   - Folder structure
   - Mock data JSON
   - React components:
     - ChatList
     - ChatItem
     - ChatScreen
     - MessageBubble
     - Header
     - InputBar
   - Animation logic (Framer Motion variants)
   - Tailwind styling
   - Instructions to run the prototype

6. **Constraints**
   - Must be fully interactive
   - Must run without modification
   - No external services
   - Keep UI clean, modern, and Messenger‑like
   - All animations must be smooth and subtle

7. **Deliverables**
   - Complete runnable prototype
   - All code in one response
   - Clear setup instructions

====================================================
BEGIN NOW
====================================================
